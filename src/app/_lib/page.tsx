export default function lib() {
  return (
    <h1>
      - You cannot view this page as this folder is hidden due to _ prefix - but
      if you actually want _ in your routing you can use "%5F" instead of "_" to
      add _ in your routing
    </h1>
  );
}
