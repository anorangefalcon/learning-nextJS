export default function LoginPage() {
  // these routes are logically grouped in auth folder but can directly be accessed by /login without need of /auth/login
  return (
    <div>
      <h1>Login</h1>
    </div>
  );
}
