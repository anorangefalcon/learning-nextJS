export default function RegisterPage() {
  // these routes are logically grouped in auth folder but can directly be accessed by /register without need of /auth/register

  return (
    <div>
      <h1>Register</h1>
    </div>
  );
}
